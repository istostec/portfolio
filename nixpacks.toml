[variables]
PYTHON_VERSION = "3.11"

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "cd admin",
  "python -m pip install -r requirements.txt"
]

[start]
cmd = "cd admin && gunicorn app:app"
